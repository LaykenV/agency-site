# Detailed plans

These files contain implementation detail for work that is not fully represented
by current production behavior.

`../ROADMAP.md` owns triggers and sequencing. `../ARCHITECTURE.md` owns the
implemented system. A plan becomes active only when its roadmap trigger is met
and the current code and external dependencies have been re-verified.
